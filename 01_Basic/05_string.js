const name = "meraj"
const repoCount = 50;

//print string using string concatination
// console.log(name + repoCount + "value");

// print string using string interpolation
//it use batstic to define all string and use veriable inline using $ sign and curle brashe -- 
// console.log(`my name ${name} and my repocont in github is ${repoCount}. `);

const MyStr = new String("Meraj Siddique")
// console.log(MyStr.charAt(2));
// console.log(MyStr.includes("er"));
// console.log(MyStr.replace("er","at"));
console.log(MyStr.slice(2,4));
console.log(MyStr.search("M"));
